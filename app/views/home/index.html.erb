<ul>
  <%- @users.each do |user| %>
    <li>
      <%= "#{user.name} created a #{user.todolists[0].name}
            wishlist with #{user.todolists[0].affairs.count} wishes" %>
    </li>
  <% end %>
</ul>